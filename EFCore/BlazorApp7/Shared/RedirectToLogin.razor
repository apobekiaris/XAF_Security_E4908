@inject NavigationManager Navigation

@code {
    protected override void OnInitialized() 
        => Navigation.NavigateTo($"authentication/WebAPILogin?returnUrl={Uri.EscapeDataString(Navigation.Uri)}");
}
